# MOS_BOT version ${MAJOR_VERSION}.${MINOR_VERSION}.${PATCH_VERSION}

Fix "CouldNotDetermineInstances" startup error:

Simultaneous instance checking is disabled because programs built with PyInstaller are multiprocess by default.

# Correções e melhorias à fazer

FAIL - Atualização do ChromeDriver dando problema com certificado da rede NetSkope em `chromedriver_get_remote_version`
DONE - Problema dos espaços no caminho do navegador Google Chrome em `googlechrome_get_local_version`
DONE - Usar a configuração `ODLPATH` para arquivos gerado em `sharepoint_scrapper` e tornar opcional
DONE - Tornar a configuração `TMPPATH` opcional (criar e usar a pasta `tmp` na pasta BASE_FOLDER)
